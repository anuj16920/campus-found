import express from 'express';

const router = express.Router();

// Auth routes removed - no Supabase auth
// Supabase storage is still used via server/src/config/supabase.js

export default router;
